// -- CodeEditor.tsx --

import "./codeEditor.css";
import MonacoEditor from "@monaco-editor/react";

const CodeEditor = () => {
  return <MonacoEditor className="MonacoEditor" />;
};

export default CodeEditor;
