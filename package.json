{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start:cli": "lerna run start --parallel",
    "start:web": "lerna run start --scope=@j-scribe1/local-client",
    "build:cli": "lerna run build --parallel",
    "build:web": "lerna run build:web --scope=@j-scribe1/local-client"
  },
  "devDependencies": {
    "lerna": "^6.6.1"
  }
}
